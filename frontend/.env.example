# Backend API Base URL
VITE_API_URL="http://localhost:8000/api/v1"

# Mapbox Access Token (Optional: if blank, open basemap fallback is loaded)
# Get a free token at https://account.mapbox.com/
VITE_MAPBOX_TOKEN=""
